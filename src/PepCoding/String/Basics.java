package PepCoding.String;

import java.util.Scanner;

public class Basics {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
       // String s1 = "hello";
        //String s1 = sc.next();//take input till space
        //String s2 = sc.nextLine();//take input over the line
        /*System.out.println(s1);
        System.out.println(s2);
        System.out.println(s2.length());// array me length datamember hota hai yeha string me method() hai.
        s.charAt(index)//give character at particular index.

        String s = "abcd";
        System.out.println(s.substring(0,2);//print substring from "i t0 j-1" i.e here 0th and 1th, not char 0f 2.
        System.out.println(s.substring(1));//print substring starting from index 1.
        //que. print all substring
        for (int i = 0; i < s.length(); i++){
            for (int j = 1 + i; j< = s.length(); j++){
                System.out.println(s.substring(i, j));
             }
         }


        String s = "abc, def, ghi, jkl, mno";
        String [] parts  = s.split(",");
        for (int  i = 0 i< parts.length; i++){
            sout(parts[i]);
        }
        */


    }
}
